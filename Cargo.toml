[package]
name = "coke_oven_system"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"] # 编译为 C 兼容动态库

[dependencies]
rusqlite = { version = "0.29.0", features = ["bundled"] }
chrono = "0.4.31"
libc = "0.2.147"

[dev-dependencies]
tempfile = "3.3.0"
